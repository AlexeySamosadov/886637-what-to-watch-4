import React from "react";
import Main from "../main/main.jsx";

// eslint-disable-next-line react/prop-types
const App = ({data}) => {
  return (
    <Main
      data={data}
    />
  );
};

export default App;
